apiVersion: cert-manager.io/v1alpha2
kind: Certificate
metadata:
  name: prometheus-imroc-io
  namespace: monitoring
spec:
  secretName: prometheus-imroc-io-tls
  issuerRef:
    name: letsencrypt-dns01
    kind: ClusterIssuer
  dnsNames:
  - prometheus.imroc.io
